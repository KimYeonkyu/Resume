{
  "name": "jin-kim-cloudflare-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:dist": "node scripts/check-dist.mjs",
    "deploy": "npm run build && npm run check:dist && wrangler deploy",
    "r2:upload": "node scripts/upload-protected-media.mjs",
    "test:worker": "vitest run tests/worker",
    "test:browser": "npm run build && PYTHONDONTWRITEBYTECODE=1 pytest -q -p no:cacheprovider tests/test_portfolio_access.py tests/test_portfolio_video.py",
    "test:runtime": "npm run build && vitest run --config vitest.worker.config.ts",
    "types": "wrangler types worker-configuration.d.ts",
    "typecheck": "wrangler types worker-configuration.d.ts --check && tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.20.1",
    "@types/node": "22.20.1",
    "tailwindcss": "3.4.17",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.118.0"
  }
}
